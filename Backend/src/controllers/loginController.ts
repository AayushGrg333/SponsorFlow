import { RequestHandler } from "express";
import passport from "passport";

const loginController : RequestHandler  = async (req,res,next) =>{
     try {
          const { identifier , password , usetype } = req.body;
          


     } catch (error) {
          
     }
}